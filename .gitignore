# Secrets — never commit
.env
config_secrets.py
*.local

# Filled SD first-boot config carries WiFi + pi passwords — only the .example is committed
custom.toml
scripts/custom.toml

# Python
env/
venv/
__pycache__/
*.pyc

# SD card images / backups
*.img
*.img.gz
*.bak

# OS
.DS_Store
