# Secrets — never commit
.env
config_secrets.py
*.local

# Filled SD first-boot config carries WiFi + pi passwords — only the .example is committed
custom.toml
scripts/custom.toml

# Tailscale reusable auth key (secret) — only the .example is committed
scripts/vestor-tailscale.auth
vestor-tailscale.auth
*.authkey

# Python
env/
venv/
__pycache__/
*.pyc

# SD card images / backups
*.img
*.img.gz
*.bak

# Emulator render-test artifacts (tools/emulator_capture.py)
emulator_config.json
emu_frames/

# OS
.DS_Store
